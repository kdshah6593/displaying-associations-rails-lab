<h1>Artists</h1>

<% @artists.each do |artist| %>
    <div>
        <p><%= link_to artist.name, artist_path(artist) %></p>
        <p><%= pluralize(artist.song_count, "song")%></p>
    </div>
<% end %>